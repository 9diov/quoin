---
_type: "[[concept]]"
title: Welcome
---

# Welcome

An instance of concept. The default validate pass dispatches this through
the registry and validates it against `types/concept.md`. The meta-type
constraint that concept itself must declare an `icon` is exercised by the
explicit-target manifest entries.
